# Code Review Evidence — PRJ-924

**Branch:** `feature/PRJ-924-deleted-polish`
**Files changed:** `src/routes/deleted.tsx`, `src/routes/deleted.test.tsx`
**Test result:** 168/168 pass (15 test files)
**Type check:** `tsc -b` clean

---

## Findings

### LOW|deleted.tsx:195|IIFE pattern in JSX for button state
The IIFE inside JSX for computing `expired`, `parentDeleted`, `disabled`, `title` is functional but slightly noisy. Consider extracting a small `<RestoreButton>` sub-component in a future refactor. Not blocking.

### LOW|deleted.test.tsx:330|Previously-broken test structure
The PRJ-923 test (`shows delete reason, displayName, and breadcrumb path`) was outside the `describe` block due to a stray `});` at line 330. Fixed as part of this PR — the `});` is now at EOF where it belongs.

---

## Verification

| Check | Result |
|---|---|
| Fix 1: Item restore disabled when parent folder deleted | ✅ Test `pre-disables item Restore when parent folder is deleted` passes |
| Fix 1: Folder restore disabled when its parent deleted | ✅ Test `pre-disables folder Restore when its parent is deleted` passes |
| Fix 1: Tooltip reads "Restore the parent folder first" | ✅ Asserted in both new tests |
| Fix 2: Folder parent path no longer duplicates segment | ✅ Test `does not duplicate folder parent path segment` passes |
| Fix 2: `ancestors` used directly, no `.concat(parentId)` | ✅ Confirmed in diff |
| Product invariants preserved | ✅ No rules changes, no data model changes, no QR/roll-count/stock-audit surface touched |
| Type safety | ✅ `tsc -b` clean |
| Test coverage | ✅ 12 route tests (was 9), +3 new |

---

## Product Invariant Checklist

- [x] 1 roll = 1 item — no change
- [x] Permanent QR — no change
- [x] Stock adjustments require actor+reason+old+new — no change
- [x] Folders support ≥4 levels — no change
- [x] Movement history append-only — no change
- [x] Deleted items 7-day soft-delete window — no change

---

**APPROVE**
