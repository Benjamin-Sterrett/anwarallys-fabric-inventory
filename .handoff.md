# Session Handoff — Anwarallys Fabric Inventory

**Last updated:** 2026-04-27 (after PRJ-873/874/877 + PRJ-783 + PRJ-784 merged — Waves 1 + 2 100% done)
**Resume command:** "resume anwarallys"

---

## TL;DR for the next session

**Waves 1 + 2 are 100% shipped.** Auth UX, folder browse/create, item create/edit are all live. The user can sign in, navigate folders, create items, edit non-historical fields. Stock adjustment (the single most important correctness ticket on the project) is **next**: PRJ-787 with `runTransaction`.

**Three more PRs merged in the 2026-04-27 session (after the Wave 1 close-out):**
- **PR #15** (squash `57c9e45`) — `fix(PRJ-873,PRJ-874,PRJ-877)`: Rules + auth defense-in-depth follow-ups. Bundle of three deferred findings from PR #10/#11/#13. firestore.rules `.lower()` email parity (PRJ-873), admin self-deactivation/self-delete guards on `/users/{uid}` (PRJ-874), `sanitizeContinue` rejects `/login` (PRJ-877). 4 files / +31 LOC. Lead Codex clean APPROVE — no findings.
- **PR #16** (squash `f9d6634`) — `feat(PRJ-783)`: folder browse + create. New `createFolder`, `subscribeToFolderChildren`, `getFolderById` in queries/folders.ts. New `src/routes/folder.tsx` handles both root (`/`) and `/folders/:id` with depth-driven UX (search at depth ≥4 = only hardcoded check). 4 files / 499 LOC. Teammate Codex caught HIGH `ancestors` double-append + MEDIUM auth race in round 1, fixed by round 2. Lead Codex deferred 1 P2 (Retry button doesn't retry metadata fetches) → PRJ-878.
- **PR #17** (squash `35757b2`) — `feat(PRJ-784)`: roll item create/edit form. New `createItem`, `updateItem`, `listActiveItemsInFolder`. New `src/routes/item-form.tsx` (shared create/edit). 6 files / 539 LOC (teammate self-recorded `owner_override` for ceiling — splitting would have orphaned the edit form). Lead Codex deferred 2 P2s → PRJ-879 (live items snapshot, also flagged by teammate) + PRJ-882 (edit-mode parent folder check). Schema reconciliation finding: ticket field names diverged from `RollItem` schema; teammate followed schema (sku not name, initialMeters not originalLengthMeters, etc.). Update tickets going forward to match.

**Most recent PR (2026-04-27 session):**
- **PR #13** (squash `bba3b9d`) — `feat(PRJ-781)`: Auth UX. New `/login` route, `<RequireAuth>` / `<RequireAdmin>` reusable guards (staff.tsx refactored to delegate), `AuthBar` in app shell with displayName-from-Firestore + sign-out confirm dialog, `signIn` helper. 8 files / 498 LOC. Teammate Codex APPROVE 0.89, 1 MEDIUM fixed in-PR (displayName pivoted to Firestore source — Auth profile is never updated by `createStaffUser`/`renameStaffUser`, filed as PRJ-876), 1 lead Codex P2 deferred under `owner_override` → **PRJ-877** (`sanitizeContinue` should reject `/login` itself to prevent redirect loop — bug requires malformed input, no app code constructs it).

**Three PRs merged in the 2026-04-26 session:**
- **PR #9** (squash `3223f5a`) — `fix(PRJ-865,PRJ-866)`: Firestore Rules defense-in-depth follow-ups from PRJ-805 review. `keys().hasAll(['deletedAt','deletedBy','deleteReason'])` field-presence guards on items+folders create/update; `/config/{document}` locked down to deny-all. Clean lead Codex APPROVE, no findings.
- **PR #10** (squash `fdac2fd`) — `feat(PRJ-856)`: Staff data layer + admin gate (PR A). 8 files, 496 LOC. New `User` model + converter; `src/lib/queries/users.ts` (7 functions including `reactivateStaffUser` and `listInactiveStaff` per Codex finding); `src/lib/auth/isAdmin.ts` case-insensitive admin gate; `.github/workflows/deploy.yml` wiring `VITE_ADMIN_EMAIL` from GH Actions vars; `src/vite-env.d.ts` + `.env.example`. 4 mid-impl Codex rounds with HIGH findings fixed in-code (orphan Auth user on Firestore-write failure, over-eager rollback, ambiguous create rejections, irreversible deactivation). 1 lead Codex P2 deferred under `owner_override` → **PRJ-873**.
- **PR #11** (squash `f689420`) — `feat(PRJ-871)`: `/staff` page UI completes PRJ-856. 2 files, 496 LOC. New `src/routes/staff.tsx` (494 LOC) consuming all 6 data-layer functions + `isAdminEmail` + `subscribeToAuthState`; `src/routes/router.tsx` registers `/staff`. Mid-impl Codex found HIGH (admin self-deactivation) + MEDIUM (plaintext password input) — both fixed in-code. Defense-in-depth filed as **PRJ-874**. 1 lead Codex P1 deferred under `owner_override` → same finding as PR #10 (PRJ-873).

**Bootstrap admin config (lead did this manually via CLI before PR #10):**
- GitHub Actions repo variable `VITE_ADMIN_EMAIL=shaaizladhani@gmail.com` (lowercase)
- Firebase Auth: enabled (Console click), Email/Password provider on, user `V000bembaURvXdqohY1DbAwlnB52` created with `email_verified: true`
- Firestore: `/config/admin.adminEmail = "shaaizladhani@gmail.com"` (lowercase) and `/users/V000bembaURvXdqohY1DbAwlnB52` with `displayName: "Shaaiz", isActive: true`
- Temp password in macOS Keychain: `~/.claude/scripts/keychain-secrets.sh get anwarallys-shaaiz-bootstrap-password`
- Documented in `~/.claude/reference/secrets-inventory.md`

**Next concrete action when you resume:**
- **Wave 3 starts: PRJ-787 (stock adjustment) is THE high-stakes ticket of the project.** Per CLAUDE.md: "bug here = wrong stock, the one thing the project exists to prevent." Claude required for the `runTransaction` skeleton; Kimi can implement within Claude's locked plan once that exists. After PRJ-787, PRJ-788 (reason chip) + PRJ-789 (movement history) round out Wave 3.
- **Wave 4 = first Kimi pilot:** PRJ-792 (QR generation). Designated as the safest first Kimi run — small, isolated, code sample literally in synthesis §2, visually verifiable.
- **Pre-pilot follow-up cluster (PRJ-878, 879, 880, 881, 882):** all small UX/test/index follow-ups from Wave 2. Bundle into 1-2 PRs before pilot. PRJ-876 (sync Firebase Auth displayName when staff renamed) also pre-pilot.
- **Smoke-test the live deploy** if not done yet: sign in as Shaaiz (creds in Keychain `anwarallys-shaaiz-bootstrap-password`), navigate folders, create an item, sign out, etc.

**Do not re-litigate any locked decisions** — the synthesis revisions table at `research/synthesis.md` (bottom) is the source of truth.

**Open architectural debates:** PRJ-840 (read/write model split — held unified for v1).

---

## Project at a glance

Clean rebuild of a fabric-roll inventory app for one Indian retail fabric store (Anwarallys, owned by Shaaiz Ladhani). 2-3 staff on Android tablets/phones. **This is a portfolio project for Benjamin** — being built cheap, must be reliable + zero-upkeep for the client long-term. Client paying very little, so cost discipline matters: ride **Kimi** for most implementation work (with checkpoints), Claude for strategic + security-critical tickets only.

## Infra ownership (locked)

| Surface | Owner | Status |
|---|---|---|
| GitHub repo `Benjamin-Sterrett/anwarallys-fabric-inventory` | Benjamin | **Public** (free Actions, portfolio visibility) |
| Cloudflare Pages | Client (`Shaaizladhani@gmail.com` account, ID `74465de04ed5c39b43ad9ffc8b47bc08`) | Old prototype `anwarallysinventory.pages.dev` lives here. **Client needs to OAuth Cloudflare to GitHub repo** to enable git-push deploys for the new app |
| Firebase project `anwarallys-inventory` | Client | Editor access granted to `bens.hph@gmail.com`; Firestore region `asia-south1`; web app already registered (App ID `1:408639124434:web:e08e1d2be1729d8cbcce0b`); Authentication enabled with Email/Password provider; `/config/admin` + bootstrap user + `/users/{uid}` all seeded |

Why split this way: Benjamin gets portfolio credit (repo on his GH), client owns the running infra long-term so if Benjamin stops maintaining, the client still owns deploy + data.

---

## Locked architecture (full detail in `research/synthesis.md`)

**Data model:**
- Flat top-level collections: `/folders/`, `/items/`, `/movements/`, `/deletedRecords/`, **`/users/`**, `/config/`
- Folder tree via `parentId + ancestors[]` (IDs, not names — survives renames)
- `folderAncestors[]` denormalized on items for subtree queries
- **No cached `activeItemCount`** — on-demand `count()` aggregation prevents drift
- TS interfaces locked in synthesis §1; `User` shipped in PRJ-856 PR A

**Stock writes:**
- **`runTransaction()` wraps item update + movement creation atomically.**
- Block writes when offline (pilot policy)
- Pessimistic UI: button disabled with spinner until 2xx
- 10-sec timeout surfaces failure dialog regardless

**QR:**
- `qrcode.react` `<QRCodeSVG>`, **Level Q**, SVG only
- Permanent URL `https://<host>/i/{firestoreAutoId}` — **NEVER regenerated**

**Auth (PRJ-856 + PRJ-871 shipped):**
- Email/password + LOCAL persistence (`indexedDBLocalPersistence`)
- **Device-bound login** — sign in once per device
- **Two roles only:** `admin` (email matches `/config/admin.adminEmail` AND `request.auth.token.email_verified == true`) vs everyone else
- Admin self-services staff via `/staff` page (PRJ-856 + PRJ-871). `createStaffUser` uses a transient secondary Firebase Auth instance so the admin's primary session isn't switched.
- Bootstrap done — see Current Status above.
- **Known parity gap (PRJ-873):** firestore.rules `isAdminUser()` compares email case-sensitively; client `isAdminEmail()` lowercases both sides. Currently mitigated by ensuring `/config/admin.adminEmail` is always lowercase. PRJ-873 fixes durably with `.lower()` in Rules.
- **Known defense-in-depth gap (PRJ-874):** admin can deactivate themself. Client guard shipped in PR #11; Rules guard is the durable fix.

## Non-negotiable invariants

- 1 roll = 1 item; **never introduce a separate editable roll count**
- Permanent QR per item — never changes on edit
- Stock adjustments require actor + reason/note + old + new written **atomically** (`runTransaction`)
- Folders support ≥4 levels with **no hardcoded depth logic**
- Movement history is **append-only** (Security Rules enforce)
- 7-day soft-delete window with restore preserves history
- **Firebase Admin SDK NEVER in app path**
- All client-side writes must surface success/failure visibly — **never silent fail**
- `/users/{uid}` writes go through admin-gated client SDK only (no Admin SDK)

## Linear ticket state

**Done (2026-04-27 session):**
- PRJ-781 — MERGED PR #13. Wave 1 100% done.
- PRJ-873 + PRJ-874 + PRJ-877 — MERGED PR #15. Defense-in-depth bundle.
- PRJ-783 — MERGED PR #16. Wave 2 starts.
- PRJ-784 — MERGED PR #17. Wave 2 done.

**Done (2026-04-26 session):**
- PRJ-865 + PRJ-866 — MERGED PR #9
- PRJ-856 (PR A) + PRJ-871 (PR B) — both MERGED, PRJ-856 main ticket closed

**Done (prior sessions):**
- PRJ-778 (Scaffold), PRJ-779 (Schema), PRJ-835 (CI auto-deploy), PRJ-780 (Firebase boundary), PRJ-854/855 (boundary follow-ups), PRJ-805 (Security Rules), plus Codex-finding tickets PRJ-857..863 (PR-805) and PRJ-843..845, 847..852 (PR-780).

**Filed in 2026-04-27 session (closed):**
- **PRJ-877** — sanitizeContinue loop guard (CLOSED in PR #15).

**Filed in 2026-04-27 session (open):**
- **PRJ-875** — route-level auth tests (blocked on PRJ-841 Vitest setup).
- **PRJ-876** — sync Firebase Auth `displayName` when `createStaffUser`/`renameStaffUser` runs (parity with Firestore `/users/{uid}.displayName`).
- **PRJ-878** — `folder.tsx` Retry button should also retry folder metadata fetches (P2 from PRJ-783 lead Codex).
- **PRJ-879** — Live items snapshot in folder browse (replace one-shot `getDocs` with `onSnapshot`). P2 from PRJ-784 lead Codex; teammate also flagged.
- **PRJ-880** — Items list paging (cursor-based, default 50/page). P2 from PRJ-784 teammate Codex.
- **PRJ-881** — Tests for `createItem`/`updateItem` write boundary (blocked on PRJ-841).
- **PRJ-882** — `item-form.tsx` validate parent folder is active before edit submit (P2 from PRJ-784 lead Codex).

**Filed in 2026-04-26 session:**
- **PRJ-873** (MEDIUM) — firestore.rules: normalize email casing in `adminEmail()` comparison via `.lower()`. Compensating action for the Codex P1/P2 findings deferred in PR #10 + #11 under `owner_override`. Operational mitigation already applied (lowercased seed + GH var). **Address before pilot launch.**
- **PRJ-874** (defense-in-depth) — Rules: prevent admin self-deactivation. Client guard shipped in PR #11; Rules is the durable fix.
- **PRJ-872** — Vitest tests for `createStaffUser` rollback paths + `isAdminEmail` casing (blocked by PRJ-841).

**Open priorities:**
- **PRJ-787** — Wave 3 starts. Stock adjustment with `runTransaction`. **NEXT. Highest stakes ticket on the project.** Claude required for the transaction skeleton.
- **PRJ-788** + **PRJ-789** — Wave 3 finishes (reason chip + movement history view).
- **PRJ-792** — Wave 4 starts and **first Kimi pilot ticket**. QR generation, isolated, visually verifiable.
- **PRJ-876** + **PRJ-878** + **PRJ-879** + **PRJ-880** + **PRJ-881** + **PRJ-882** — UX/test/index follow-up cluster from Wave 2. Bundle into 1-2 PRs before pilot.
- **PRJ-804** — pre-implementation validation checklist (gates everything; physical checks needed from Benjamin/client).
- **PRJ-841 + PRJ-842** — Vitest + ESLint follow-ups.
- **PRJ-836** — bump GitHub Actions to Node 24 before September 2026.
- **PRJ-806** — iOS Safari auth verification (priority LOW post Android-primary).

**Wave 2:** PRJ-783 folder browse, PRJ-784 item form
**Wave 3:** PRJ-787 stock adjust, PRJ-788 reason, PRJ-789 movement history
**Wave 4:** PRJ-792 QR gen, PRJ-793 QR print, PRJ-794 scan landing
**Wave 5:** PRJ-796 soft-delete, PRJ-797 restore, PRJ-798 low-stock, PRJ-799 errors
**Wave 6:** PRJ-800/801 pilot

## Top risks (still active)

1. **PRJ-873 + PRJ-874 must ship before pilot.** Both are real defense-in-depth gaps. Client-side guards are in place; Rules fixes are the durable layer.
2. **`runTransaction` correctness in PRJ-787** — bug here = wrong stock.
3. **Tap-through muscle memory** defeats the confirmation modal — 800ms hold may need to climb to 1200ms after pilot testing.
4. **Older Android device perf** — primary platform per client.

## Bootstrap admin recap (for reference; already done)

- Run `gh variable list --repo Benjamin-Sterrett/anwarallys-fabric-inventory` — should show `VITE_ADMIN_EMAIL=shaaizladhani@gmail.com`.
- Firebase Console → Authentication → Users — should show one user (Shaaiz, UID `V000bembaURvXdqohY1DbAwlnB52`, email verified ✓).
- Firestore Console → `/config/admin` — should show `adminEmail: "shaaizladhani@gmail.com"`.
- Firestore Console → `/users/V000bembaURvXdqohY1DbAwlnB52` — should show `displayName: "Shaaiz"`, `isActive: true`, plus audit fields.
- Temp password: `~/.claude/scripts/keychain-secrets.sh get anwarallys-shaaiz-bootstrap-password`.

## Telegram

- Project forum topic: **2580** (in `Benjamin and Uplink Control` supergroup, `-1003817797037`)
- Bot token: keychain (`uplink` / `telegram-bot-token`)
- Send pattern: write body to file, use `--data-urlencode "text@/path/to/file"`. See memory `telegram-send-pattern.md`.

## Files of record

| File | Purpose |
|---|---|
| `CLAUDE.md` | Project status hub, locked product decisions, wave plan |
| `AGENTS.md` | Codex reviewer persona with non-negotiable product invariants |
| `research/synthesis.md` | Full architecture + UX picks; client validation revisions table at the bottom |
| `firestore.rules` | Full authz surface (PRJ-805 + PRJ-865/866) |
| `firestore-rules-validation.md` | Red-team test scenarios |
| `src/lib/auth/isAdmin.ts` | Client admin gate (case-insensitive) |
| `src/lib/queries/users.ts` | Staff CRUD (data layer) |
| `src/routes/staff.tsx` | Staff page (UI) |

**Memory files:**
- `~/.claude/projects/-Users-benjaminsterrett-Projects-anwarallys-fabric-inventory/memory/MEMORY.md` is the index
- Includes: project philosophy, infra ownership, client answers 2026-04-25, telegram send pattern, deploy mechanism, don't-gate-per-step preference

## Dev workflow rules (mandatory)

- All coding changes go through `dev-workflow` skill — Standard path for auth/security
- Agent Team is the only execution model
- One sub-ticket = one branch = one PR ≤500 LOC (lockfile excluded)
- Codex must APPROVE PR before merge (or `owner_override` exception with compensating action)
- All review findings → Linear issues
- Context7 doc lookup mandatory before coding with any library
