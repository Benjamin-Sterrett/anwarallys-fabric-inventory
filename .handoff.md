# Session Handoff — Anwarallys Fabric Inventory

**Last updated:** 2026-04-26 (after PRJ-779 + PRJ-835 merged)
**Resume command:** "resume anwarallys"

---

## TL;DR for the next session

Wave 1 is rolling. **Schema (PRJ-779) merged**, **GitHub Actions auto-deploy (PRJ-835) merged**. Production URL `anwarallysinventory.pages.dev` is live and updates automatically on every merge to main. Client never had to touch GitHub — token-based deploy is fully on Benjamin's side.

**Next concrete action when you resume:**
- Start **PRJ-780** (Firebase data-access boundary). Imports the new types from `src/lib/models/`. Wires `getFirestore` with `persistentLocalCache + persistentMultipleTabManager`, defines the read/write helper layer. Foundational — Claude implements.
- After PRJ-780: PRJ-805 (Security Rules — Claude only, CRITICAL) can run in parallel. Then PRJ-781 (auth, blocked on client supplying 2-3 staff nicknames).

**Do not re-litigate any locked decisions** — the synthesis revisions table at `research/synthesis.md` (bottom) is the source of truth, updated 2026-04-25 with client answers.

**One open architectural debate for later:** PRJ-840 captures the read/write model split question that came up during PRJ-779 review. Codex pushed for splitting every type into `*Read`/`*Write`. We held the unified shape (matches synthesis spec, Firebase TS community convention, v1 simplicity). Revisit if pilot surfaces real misuse.

---

## Project at a glance

Clean rebuild of a fabric-roll inventory app for one Indian retail fabric store (Anwarallys, owned by Shaaiz Ladhani). 2-3 staff on Android tablets/phones. **This is a portfolio project for Benjamin** — being built cheap, must be reliable + zero-upkeep for the client long-term. Client paying very little, so cost discipline matters: ride **Kimi** for most implementation work (with checkpoints), Claude for strategic + security-critical tickets only.

## Infra ownership (locked)

| Surface | Owner | Status |
|---|---|---|
| GitHub repo `Benjamin-Sterrett/anwarallys-fabric-inventory` | Benjamin | **Public** (free Actions, portfolio visibility) |
| Cloudflare Pages | Client (`Shaaizladhani@gmail.com` account, ID `74465de04ed5c39b43ad9ffc8b47bc08`) | Old prototype `anwarallysinventory.pages.dev` lives here. **Client needs to OAuth Cloudflare to GitHub repo** to enable git-push deploys for the new app |
| Firebase project `anwarallys-inventory` | Client | Editor access granted to `bens.hph@gmail.com`; Firestore region `asia-south1`; web app already registered (App ID `1:408639124434:web:e08e1d2be1729d8cbcce0b`) |

Why split this way: Benjamin gets portfolio credit (repo on his GH), client owns the running infra long-term so if Benjamin stops maintaining, the client still owns deploy + data.

## What shipped (PRJ-778, PR #1, squash `2b5906d3`)

Scaffold is live. Stack:
- Vite 6.4 + React 19.2 + TS 5.9 strict (`noUncheckedIndexedAccess`)
- Tailwind v4.2 via `@tailwindcss/vite`
- react-router-dom 7.14 data router, 7 placeholder routes + 404
- Firebase 10.14 client SDK with **HMR-safe singleton** (`getApps()` guard) at `src/lib/firebase/app.ts`
- pnpm 9.15 (`packageManager` field is single source of truth)
- `public/_redirects` SPA fallback, `public/favicon.svg` branded magenta "A", manifest wired
- CI runs typecheck + build (no fake-green test step)
- Bundle: 91.78 KB gzipped JS

5 Codex review rounds during merge. Lessons learned baked in. PRJ-807 closed during PR-778 final pass.

## Locked architecture (full detail in `research/synthesis.md`)

**Data model:**
- Flat top-level collections: `/folders/`, `/items/`, `/movements/`, `/deletedRecords/`
- Folder tree via `parentId + ancestors[]` (IDs, not names — survives renames)
- `folderAncestors[]` denormalized on items for subtree queries
- **No cached `activeItemCount`** — on-demand `count()` aggregation prevents drift
- TS interfaces locked in synthesis §1

**Stock writes:**
- **`runTransaction()` wraps item update + movement creation atomically.** Two writes that can succeed independently = wrong-stock bug.
- Block writes when offline (pilot policy — Wi-Fi confirmed unreliable in storerooms)
- Pessimistic UI: button disabled with spinner until 2xx, then UI updates
- 10-sec timeout surfaces failure dialog regardless

**QR:**
- `qrcode.react` `<QRCodeSVG>`, **Level Q**, SVG only
- Permanent URL `https://<host>/i/{firestoreAutoId}` — **NEVER regenerated** on edit
- Scan landing is **READ-ONLY** — explicit "Adjust stock" button to enter mutation flow
- Default OS camera handles scanning; no in-app scanner

**Auth (UPDATED post client answers):**
- Email/password + LOCAL persistence (`indexedDBLocalPersistence`)
- **Device-bound login** — sign in once per device, stay logged in, all changes tagged with that device's logged-in user
- **No "Who is doing this?" actor selector** at every change
- 2-3 fixed nicknames seeded by client (TBD names)

**UX:**
- 48×48 px min touch targets, 12px spacing
- Hybrid numeric input: "Sold/used" (subtract) vs "Set to exact" (direct), `inputmode="decimal"`, ± stepper at 0.5m
- Drill-down folder nav with breadcrumb chips for depth ≥3, search at depth ≥4
- Confirmation modal: 32px old→new + reason chip + hold-to-confirm 800ms + button label shows transition
- 15s undo snackbar after save (everyone gets it)
- Reason chip selector required before Save enabled (Sold/Cut/Damage/Return/Correction/Receive/Other)
- Traffic-light global sync indicator in app bar (🟢 Online / 🟡 Syncing / 🔴 Error)
- ESL-friendly copy: icon+text labels, Grade 5-6 reading level, no idioms, plain-language errors

## Client validation answers (2026-04-25, locked)

Full table in `research/synthesis.md` "Client validation 2026-04-25" section. Highlights:

| Topic | Locked |
|---|---|
| Primary platform | **Android first** (older tablets/phones), iOS for completeness only |
| Label printer | 5–6k INR thermal (~203 dpi). **Drop 20mm option.** 50mm default, 30mm fallback |
| Auth model | **Device-bound login**, no per-change actor (simplification of PRJ-781) |
| Categories | **No predefined categories.** Folder-only org. Optional `type: string` user tag |
| Scale | Pilot: 500 items. Target: 5,000. Within design envelope |
| Roll length range | 15m–100m typical. Soft validation: warn outside ~5m–200m |
| Low-stock threshold | **Global 10m default.** `minimumMeters` field stays in schema as future override hook (not exposed in v1 UI) |
| Undo | Available to everyone (no role gate) |
| Old app | Nothing to preserve |

**Q16 sticker reprint concern resolved:** client worried about reprinting on every cut. Reality: sticker is identity-only (QR), meters live in app. Reassurance text saved + sent to Telegram for client. Client should now understand cut → app update, no new sticker.

**Still pending from client:**
1. 2-3 staff nicknames (he'll provide)
2. WhatsApp answers to questions 5 & 6 (label sizes specifics, label adhesion to rolls) — can defer to PRJ-804 physical validation
3. OAuth Cloudflare Pages to the GitHub repo so deploys auto-trigger

## Non-negotiable invariants (Codex enforces; AGENTS.md cites)

- 1 roll = 1 item; **never introduce a separate editable roll count**
- Permanent QR per item — never changes on edit
- Stock adjustments require actor + reason/note + old + new written **atomically** (`runTransaction`)
- Folders support ≥4 levels with **no hardcoded depth logic**
- Movement history is **append-only** (Security Rules enforce)
- 7-day soft-delete window with restore preserves history
- **Firebase Admin SDK NEVER in app path** (breaks Workers runtime)
- All client-side writes must surface success/failure visibly — **never silent fail**
- Errors never silent — pessimistic UI + MD3 dialog on failure + 10-sec timeout

## Top risks (still active)

1. **Firestore Security Rules** are the entire authz surface — PRJ-805 must be red-teamed before pilot. **Claude-only ticket.**
2. **`runTransaction` correctness in PRJ-787** — bug here = wrong stock, the one thing the project exists to prevent. Claude plans + reviews carefully even if Kimi implements.
3. **Tap-through muscle memory** defeats the confirmation modal — 800ms hold may need to climb to 1200ms after pilot testing.
4. **Older Android device perf** — primary platform per client. Test on Android 10/11 low-end before pilot.

## Linear ticket state

**Done:**
- PRJ-778 (Scaffold) — MERGED
- PRJ-779 (Schema) — MERGED 2026-04-26 (PR #3, squash `1767d78`). 7 Codex review rounds. 6 findings fixed in-PR, 3 deferred under `owner_override` exception (read/write model split deemed out of scope for v1; tracked as PRJ-840). Single file `src/lib/models/index.ts`, 169 LOC.
- PRJ-835 (CI auto-deploy) — MERGED 2026-04-26 (PR #2, squash `b7c299a`). GitHub Actions deploys to Cloudflare Pages on every push to main. Replaces the manual wrangler-deploy path; client never touches GitHub.
- PRJ-807 (favicon follow-up) — closed in PRJ-778 final commit

**Follow-ups filed during this session:**
- **PRJ-836** (LOW) — bump GitHub Actions to Node 24 before September 2026
- **PRJ-840** (LOW) — Schema: revisit read/write model split if pilot surfaces real misuse

**Open priorities:**
- **PRJ-780** — Firebase data-access boundary (next up — Claude implements; consumes the new schema types)
- **PRJ-804** — pre-implementation validation checklist (gates everything; physical checks needed from Benjamin/client)
- **PRJ-805** — Firestore Security Rules (CRITICAL, Claude-only — can run parallel with PRJ-780)
- **PRJ-806** — iOS Safari auth verification (priority lowered to LOW post Android-primary)

**Wave 1 implementation queue (dependency order):**
1. ~~PRJ-779 schema~~ — DONE
2. **PRJ-780 Firebase boundary** — next, unblocks PRJ-781, PRJ-783, PRJ-784, PRJ-787
3. PRJ-805 Security Rules (parallel with PRJ-780)
4. PRJ-781 Auth (after PRJ-780, after client supplies nicknames)

**Wave 2:** PRJ-783 folder browse, PRJ-784 item form
**Wave 3:** PRJ-787 stock adjust, PRJ-788 reason, PRJ-789 movement history
**Wave 4:** PRJ-792 QR gen, PRJ-793 QR print, PRJ-794 scan landing
**Wave 5:** PRJ-796 soft-delete, PRJ-797 restore, PRJ-798 low-stock, PRJ-799 errors
**Wave 6:** PRJ-800/801 pilot

## Flight plan: Claude vs Kimi (full detail at `research/flight-plan-claude-vs-kimi.md`)

**Hard rules — Claude only:**
- PRJ-805 Security Rules (data leak risk too high for delegation)
- Lead Codex review at every PR-REVIEW step (orchestrator must be Claude)
- PRJ-787 `runTransaction` plan + scrutiny (Kimi can implement within Claude's locked skeleton)

**Everything else → Kimi as teammate, Claude as lead.** ~12 of 15 remaining implementation tickets.

**Mechanism (confirmed):** Kimi is a fully integrated drop-in `Agent()` teammate as of dev-workflow v8.22.0. `install-kimi.sh` wires hooks into `~/.kimi/config.toml`, Kimi runs the same `Skill(skill="dw-*")` sequence as Claude teammates with one diff (security review inline, no haiku subagent). Sessions output to `~/.kimi/sessions/`.

**Trust ramp on this project:**
1. **First 2-3 Kimi tickets → Pattern B' (Two-Spawn).** Spawn 1 stops after IMPLEMENT without committing → Benjamin reads `git diff` in worktree → Spawn 2 resumes for VERIFY → DELIVER → PR. Real architectural pause, not a workaround.
2. **Once 2-3 clean → Pattern A'.** Kimi runs straight-through to PR; Benjamin reviews PR diff in GitHub before Claude lead does Codex + merge.
3. **After 3-4 clean A' tickets → consider Pattern A** (lead-only review, no Benjamin checkpoint).

**Pilot Kimi ticket:** **PRJ-792 (QR generation)** via Pattern B'. Small, isolated, code sample literally in synthesis §2, visually verifiable. Run AFTER PRJ-779 (it depends on `RollItem.itemId`).

**Recommended sequence:**
1. PRJ-779 schema → **Claude** (foundational, every ticket depends on it)
2. PRJ-780 Firebase boundary → **Claude** (foundational, locked pattern)
3. PRJ-792 QR generation → **Kimi pilot via Pattern B' (Two-Spawn)** ← evaluation point

**Caveat:** dev-workflow itself is actively evolving (v8.22.0 just shipped). If a Kimi-specific hook regresses on this project, fall back to Claude rather than fight it.

## Permissions audit (all green as of 2026-04-23)

| Surface | Read | Write | Notes |
|---|---|---|---|
| GitHub repo | ✅ | ✅ | `Benjamin-Sterrett/anwarallys-fabric-inventory` (public) |
| Cloudflare Pages (client acct via wrangler OAuth on `bens.hph@gmail.com`) | ✅ | ✅ | Both Benjamin's and client's accounts visible |
| Firebase project metadata | ✅ | ✅ | `anwarallys-inventory` |
| Firestore database | ✅ | ✅ | `(default)`, region `asia-south1` |
| Firestore Rules | ✅ | ✅ | Dry-run deploy passed |
| Firestore Indexes | ✅ | ✅ | Deploy auth passed |

## Telegram

- Project forum topic: **2580** (in `Benjamin and Uplink Control` supergroup, `-1003817797037`)
- Bot token: keychain (`uplink` / `telegram-bot-token`)
- **Important send pattern:** Write message body to a file, use `--data-urlencode "text@/path/to/file"`. Bash heredoc with `'\''` apostrophe escaping silently produces empty body and Telegram returns 400. Memory: `telegram-send-pattern.md`.
- Always check `"ok":true` in curl response — HTTP exit code alone doesn't confirm delivery.

## Files of record

| File | Purpose |
|---|---|
| `CLAUDE.md` | Project status hub, locked product decisions, wave plan, dev workflow rules |
| `AGENTS.md` | Codex reviewer persona with non-negotiable product invariants |
| `research/synthesis.md` | Full architecture + UX picks with vote counts; **client validation revisions table at the bottom** |
| `research/client-questions.md` | The WhatsApp questionnaire sent to client |
| `research/discovery-*.md` | Raw 7 LLM reports (only re-read if revisiting a locked decision) |
| `research/flight-plan-claude-vs-kimi.md` | Per-ticket Claude/Kimi assignment + trust ramp + workflow patterns |
| `research/topics.md` | Initial discovery topic briefs |
| `research/MEGA-PROMPT.md` + `research/prompts-pasteable.md` | Original discovery prompts |

**Memory files** (auto-loaded each session at `~/.claude/projects/-Users-benjaminsterrett-Projects-anwarallys-fabric-inventory/memory/`):
- `project-philosophy.md` — cheap portfolio build, reliable, zero-upkeep
- `infra-ownership.md` — repo with Benjamin, infra with client
- `client-answers-2026-04-25.md` — full client answer absorption
- `telegram-send-pattern.md` — file-based curl pattern; bash heredoc breaks

## Open follow-ups (when resuming)

1. **Check WhatsApp** — has client supplied staff nicknames yet? Has he replied to the Q16 reassurance?
2. **Check Cloudflare** — did client OAuth Pages to GitHub repo? If yes, visit deployed URL, confirm SPA loads + hard reload on `/i/anything` serves Coming Soon (not 404). If no, remind him.
3. **Check Linear** — `linear issue list --team PRJ` to see if anything new came in.

If client unblocks via the above:
- Continue with PRJ-779 (Claude implements) when ready
- Run PRJ-792 as Kimi pilot when PRJ-779 schema is in main

If client still blocked: PRJ-805 (Security Rules, Claude) and PRJ-779 (schema, Claude) can both proceed without client unblock — they're foundational.

## Dev workflow rules (mandatory)

- **All coding changes go through `dev-workflow` skill** — no exceptions, even trivial
- **Agent Team is the only execution model** (lead spawns teammate(s), teammate implements with Codex gates, lead reviews + merges)
- **One sub-ticket = one branch = one PR ≤500 LOC** (lockfile excluded)
- **Codex must APPROVE PR before merge** (lead runs `codex review --base main` from worktree)
- **All review findings → Linear issues** (CRITICAL/HIGH/MEDIUM fix + issue, LOW issue only)
- **Context7 doc lookup mandatory** before coding with any library (PLAN + IMPLEMENT)

For bug fixes (not applicable yet on this project): `Skill(skill="debug-workflow")` FIRST, then dev-workflow. Hook enforces `.debug-report.md` on `fix/*` branches.

## Wave 0 reminder

Wave 0 sub-tickets (PRJ-774-777) are intake/scope-lock work. Most content already in `CLAUDE.md` + `research/synthesis.md`. Either:
- Mark Done with comment pointing to those docs (low priority cleanup)
- Or write the formal artifacts they request (`prototype-notes.md`, `v1-spec.md`, etc.)

Not blockers for Wave 1 implementation.
